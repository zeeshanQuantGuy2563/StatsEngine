[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stats_engine"
version = "0.1.0"
description = "Pure NumPy probability and statistics engine"
authors = [{ name = "ZEESHAN AHMAD ANSARI" }]
dependencies = [
    "numpy",
    "pandas"
]